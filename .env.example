# NEXUS PRIME — Environment Template
# Copy to .env and fill values

# Database
POSTGRES_PASSWORD=
POSTGRES_DB=nexus_db
JWT_SECRET=
DATABASE_URL=postgresql://postgres:PASSWORD@nexus_db:5432/nexus_db

# Auth
SESSION_SECRET=

# Stripe (UK company formation pending — leave empty until ready)
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=

# Local sovereignty (PostgREST, LiteLLM, etc.)
SUPABASE_URL=
SUPABASE_KEY=

# ═══ Ready-to-Fill (paste values when ready) ═══
# SMTP (Nerve, Shadow Seven email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=NEXUS PRIME

# Dify Bridge (after creating workflow in Dify)
DIFY_API_KEY=
DIFY_DEFENSIVE_WORKFLOW_ID=
