# ═══════════════════════════════════════════════════════════════════════════
# 🔔 NEXUS PRIME - AlertManager Configuration
# ═══════════════════════════════════════════════════════════════════════════

global:
  resolve_timeout: 5m

# ═══════════════════════════════════════════════════════════════════════════
# 📮 Routes (توجيه الإشعارات)
# ═══════════════════════════════════════════════════════════════════════════
route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default'
  
  routes:
    # Critical alerts → فوري
    - match:
        severity: critical
      receiver: 'critical-alerts'
      group_wait: 0s
      repeat_interval: 5m
    
    # Warning alerts → دورية
    - match:
        severity: warning
      receiver: 'warning-alerts'
      group_wait: 30s
      repeat_interval: 1h

# ═══════════════════════════════════════════════════════════════════════════
# 📨 Receivers (قنوات الإشعارات)
# ═══════════════════════════════════════════════════════════════════════════
receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://nexus_memory_keeper:9000/incidents/report'
        send_resolved: true

  - name: 'critical-alerts'
    webhook_configs:
      - url: 'http://nexus_memory_keeper:9000/incidents/report'
        send_resolved: true
      - url: 'http://nexus_cortex:8005/alerts/critical'
        send_resolved: true

  - name: 'warning-alerts'
    webhook_configs:
      - url: 'http://nexus_memory_keeper:9000/incidents/report'
        send_resolved: true

# ═══════════════════════════════════════════════════════════════════════════
# 🔇 Inhibit Rules (منع التنبيهات المكررة)
# ═══════════════════════════════════════════════════════════════════════════
inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'cluster', 'service']
