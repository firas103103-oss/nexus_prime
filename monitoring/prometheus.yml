# ═══════════════════════════════════════════════════════════════════════════
# 📊 NEXUS PRIME - Prometheus Configuration
# ═══════════════════════════════════════════════════════════════════════════

global:
  scrape_interval: 15s      # جمع المقاييس كل 15 ثانية
  evaluation_interval: 15s  # تقييم القواعد كل 15 ثانية
  scrape_timeout: 10s
  external_labels:
    cluster: 'nexus_prime'
    environment: 'production'

# ═══════════════════════════════════════════════════════════════════════════
# 🔔 Alert Rules
# ═══════════════════════════════════════════════════════════════════════════
rule_files:
  - '/etc/prometheus/alerts.yml'

# ═══════════════════════════════════════════════════════════════════════════
# 📮 AlertManager Configuration
# ═══════════════════════════════════════════════════════════════════════════
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

# ═══════════════════════════════════════════════════════════════════════════
# 🎯 Scrape Configurations
# ═══════════════════════════════════════════════════════════════════════════
scrape_configs:
  # 1️⃣ Prometheus نفسه
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # 2️⃣ Node Exporter (مقاييس السيرفر)
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node_exporter:9100']
        labels:
          instance: 'nexus_server'

  # 3️⃣ cAdvisor (مقاييس الـ Containers)
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['cadvisor:8080']

  # 4️⃣ PostgreSQL Database
  - job_name: 'postgres'
    static_configs:
      - targets: ['nexus_db:5432']
        labels:
          service: 'database'

  # 5️⃣ Redis
  - job_name: 'redis'
    static_configs:
      - targets: ['nexus_redis:6379']
        labels:
          service: 'cache'

  # 6️⃣ NEXUS Cortex (API Gateway)
  - job_name: 'nexus_cortex'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['nexus_cortex:8005']
        labels:
          service: 'cortex'

  # 7️⃣ Memory Keeper
  - job_name: 'memory_keeper'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['nexus_memory_keeper:9000']
        labels:
          service: 'memory_keeper'

  # 8️⃣ LiteLLM (AI Gateway)
  - job_name: 'litellm'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['nexus_litellm:4000']
        labels:
          service: 'ai_gateway'

  # 9️⃣ Ollama (AI Engine)
  - job_name: 'ollama'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['nexus_ollama:11434']
        labels:
          service: 'ai_engine'

  # 🔟 N8N (Automation)
  - job_name: 'n8n'
    static_configs:
      - targets: ['nexus_n8n:5678']
        labels:
          service: 'automation'

  # 1️⃣1️⃣ Frontend Dashboard
  - job_name: 'dashboard'
    static_configs:
      - targets: ['nexus_dashboard:5001']
        labels:
          service: 'frontend'

# ═══════════════════════════════════════════════════════════════════════════
# 🔍 Service Discovery (اختياري - للمستقبل)
# ═══════════════════════════════════════════════════════════════════════════
  # - job_name: 'docker_sd'
  #   docker_sd_configs:
  #     - host: unix:///var/run/docker.sock
  #       refresh_interval: 30s
