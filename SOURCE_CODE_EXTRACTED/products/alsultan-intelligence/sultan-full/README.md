# 👑 The Sultan AI

تطبيق شات AI بروح "السلطان" — برنامج اللسان العربي (قرآن فقط) مع حفظ المحادثات في Supabase.

## 🏗️ البنية التقنية

### Backend
- **Express.js** - خادم API بسيط وسريع
- **TypeScript** - نوعية قوية للكود
- **SULTAN Persona** - نظام ردّ موحّد (قرآن حصراً، اللسان العربي مرجعاً)
- **Supabase** - قاعدة بيانات PostgreSQL

### Frontend
- **Vite** - أداة بناء سريعة
- **React 18** - مكتبة UI حديثة
- **TypeScript** - أمان الأنواع
- **Tailwind CSS** - تصميم سريع

## 🚀 التشغيل المحلي

### المتطلبات
- Node.js 20+
- npm أو yarn
- إعداد مفاتيح المزود عبر `.env` (مزود داخلي غير مُسمّى)
- حساب Supabase (اختياري)

### 1. إعداد Backend

```bash
cd backend
npm install
```

أنشئ ملف `.env` في مجلد `backend`:
```env
GOOGLE_GENERATIVE_AI_API_KEY=your_internal_model_api_key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
NODE_ENV=development
PORT=5000
```

شغّل الـ Backend:
```bash
npm run dev
```

### 2. إعداد Frontend

```bash
cd frontend
npm install
```

أنشئ ملف `.env` في مجلد `frontend`:
```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
VITE_API_URL=http://localhost:5000
```

شغّل الـ Frontend:
```bash
npm run dev
```

افتح المتصفح على: `http://localhost:3000`

## 📦 النشر

### Railway

1. ارفع الكود إلى GitHub
2. اربط المشروع مع Railway
3. أضف متغيرات البيئة في Railway Dashboard
4. سيتم النشر تلقائياً!

### Docker

```bash
docker build -t the-sultan-ai .
docker run -p 5000:5000 --env-file .env the-sultan-ai
```

## 🗄️ إعداد Supabase

شغّل السكريبت في [supabase/schema.sql](supabase/schema.sql):

```sql
-- انسخ والصق المحتوى في Supabase SQL Editor
```

## 🌟 الميزات

- ✅ واجهة شات حديثة وسريعة
- ✅ دعم Streaming للردود الفورية
- ✅ حفظ المحادثات تلقائياً
- ✅ سجل محادثات مع إمكانية الحذف
- ✅ تصميم متجاوب (Mobile-friendly)
- ✅ دعم RTL للعربية
- ✅ أمان محسّن (Headers, CORS)
- ✅ شخصية "السلطان" — القرآن مصدر وحيد، اللسان العربي مرجع، حرارة ثابتة 0.2

## 📁 هيكل المشروع

```
THE-SULTAN/
├── backend/              # Express API
│   ├── src/
│   │   ├── routes/      # API Routes
│   │   └── server.ts    # نقطة الدخول
│   ├── package.json
│   └── tsconfig.json
├── frontend/            # Vite + React
│   ├── src/
│   │   ├── components/  # مكونات React
│   │   ├── lib/         # Supabase client
│   │   ├── types/       # TypeScript types
│   │   └── App.tsx
│   ├── package.json
│   └── vite.config.ts
├── supabase/            # Database schema
├── Dockerfile           # إعدادات Docker
├── Procfile             # إعدادات Railway
└── README.md
```

## 🔑 المتغيرات المطلوبة

### Backend (.env)
- `GOOGLE_GENERATIVE_AI_API_KEY` - مفتاح المزود الداخلي (لا يُذكر اسمه)
- `SUPABASE_URL` - رابط Supabase
- `SUPABASE_SERVICE_ROLE_KEY` - مفتاح خدمة Supabase
- `PORT` - منفذ الخادم (افتراضي: 5000)

### Frontend (.env)
- `VITE_SUPABASE_URL` - رابط Supabase
- `VITE_SUPABASE_ANON_KEY` - المفتاح العام لـ Supabase
- `VITE_API_URL` - رابط الـ Backend

## 🛠️ الأوامر المفيدة

```bash
# تثبيت كل التبعيات
npm install --prefix backend && npm install --prefix frontend

# تشغيل وضع التطوير
npm run dev --prefix backend &
npm run dev --prefix frontend

# بناء للإنتاج
npm run build --prefix backend
npm run build --prefix frontend

# معاينة البناء
npm run preview --prefix frontend
```

## 🧭 شخصية ونطاق "السلطان"

- المصدر الوحيد: نصوص القرآن الكريم.
- المرجع اللغوي: اللسان العربي (الجذر، الصيغة، السياق).
- لا مراجع خارجية ولا ذكر للمزوّد/النموذج.
- حرارة ثابتة: 0.2 لضمان الدقة وعدم العشوائية.
- منهج عام لكل السياقات: تحديد الموضوع، جمع المواضع القرآنية، بناء شبكة دلالية، إبراز فروق الألفاظ المتشابهة، خلاصة تعيد البوصلة للأصل.
- منهج خاص للأسئلة اللفظية: عدّ الورود، سياقات محورية، لفظان كثيرًا ما يُخلطان بها مع التفريق، أمثلة موجزة، خلاصة دقيقة.

## 🔌 واجهات API المهمة

- `GET /api/chat/quranic/data` — يعيد البيانات القرآنية وخصائص الشخصية (persona: SULTAN, source: Quran-only, temperature: 0.2).
- `POST /api/chat` — بثّ الردود؛ تُحقن رسالة نظام لشخصية السلطان وسياق قرآني، مع تحليلٍ تلقائي للأسئلة اللفظية.

## 📝 الترخيص

MIT License

## 👨‍💻 المطور

تم التطوير بواسطة [mrf103](https://github.com/mrf103)

---

**ملاحظة:** هذا المشروع تم تحويله من Next.js إلى Vite + React + Express لمزيد من المرونة والاستقلالية.
