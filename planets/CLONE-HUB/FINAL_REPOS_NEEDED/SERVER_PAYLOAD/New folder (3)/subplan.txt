تمام. ملاحظة صحيحة 100% 👍
نعدّل فورًا ونمشي **تنفيذيًا مضبوط**:
**قبل كل أمر أحدّد المكان بالضبط** (وين تكتب، وين تشغّل).

خلّيك تعتبر هذا **دليل تشغيل رسمي Phase 0**.

---

# 🚀 Phase 0 — كشف عام + تشغيل مؤكد (مُحدَّد الأماكن)

---

## 🧩 0.1 — استنساخ وتشغيل محلي

### 📍 المكان: **Terminal داخل VS Code (محليًا على جهازك)**

1️⃣ افتح **VS Code**
2️⃣ افتح **Terminal** من:

```
View → Terminal
```

3️⃣ نفّذ الأوامر التالية **نسخ/لصق**:

```bash
git clone https://github.com/firas103103-oss/mrf103ARC-Namer.git
cd mrf103ARC-Namer
npm install
cp .env.example .env
npm run dev
```

---

### 📍 المكان: **المتصفح (Chrome / Edge)**

* افتح:

```
http://localhost:5001
```

* أدخل كلمة المرور
* تأكد أن:

  * الصفحة تفتح
  * ما في شاشة بيضا
  * ما في تعليق

---

## 🧩 0.2 — فحص أخطاء الواجهة

### 📍 المكان: **متصفح Chrome**

1️⃣ اضغط:

```
F12 → Console
```

### المطلوب:

* ❌ لا أخطاء حمراء (Red Errors)
* ⚠️ دوّن التحذيرات فقط

📌 إذا ظهر خطأ:

* انسخ **الرسالة كاملة**
* لا تفسّر
* لا تصلّح بعد

---

## 🧩 0.3 — فحص السيرفر

### 📍 المكان: **Terminal داخل VS Code (نفس المشروع)**

اترك هذا الأمر شغّال:

```bash
npm run dev
```

### راقب:

* ❌ Crash
* ❌ Unhandled promise
* ❌ Auth bypass

📌 أي خطأ:

* انسخ السطر كامل

---

## 🧩 0.4 — فحص Realtime (WebSocket)

### 📍 المكان: **المتصفح (تبويبين)**

1️⃣ افتح الموقع في **تبويبين**
2️⃣ نفّذ فعل في التبويب الأول:

* Login
* Command
  3️⃣ راقب التبويب الثاني:
* هل الحدث يظهر فورًا؟

📌 نعم = OK
📌 لا = خطأ Realtime

---

## 🧩 0.5 — فحص Supabase

### 📍 المكان: **Supabase Dashboard (الموقع الرسمي)**

داخل مشروع Supabase:

#### تحقق من:

* **Tables** موجودة
* **RLS** مفعّلة
* **No public anon access**

### اختبار:

* نفّذ فعل من الواجهة
* راقب:

  * هل سجل جديد انضاف؟

---

## 🧩 0.6 — فحص Railway (إنتاج)

### 📍 المكان: **Terminal داخل VS Code**

إذا لم يكن مرفوع أو بدك إعادة نشر:

```bash
railway up
```

---

### 📍 المكان: **Railway Dashboard**

تحقق من:

* App = Running
* ENV موجودة
* Restart لا يكسر الجلسة
* Logs نظيفة

---

## 🧩 0.7 — توثيق الحالة (إجباري)

### 📍 المكان: **VS Code → Root Project Folder**

أنشئ ملف جديد:

```bash
touch PHASE_0_REPORT.md
```

واكتب فيه (نسخ/لصق):

```md
## PHASE 0 REPORT

### Blockers
- None / (اكتب إن وجد)

### Warnings
- (إن وجد)

### Realtime
- OK / FAIL

### Auth
- OK / FAIL

### UI
- OK / FAIL

### DB
- OK / FAIL

### Production
- OK / FAIL
```

---

## 🧩 0.8 — الإصلاح الفوري

### 📍 المكان: **VS Code (الكود فقط)**

### القاعدة:

* ❌ لا Refactor
* ❌ لا Features
* ✅ إصلاح أخطاء قاتلة فقط

---

## ⛔ ممنوع في Phase 0

* إضافة ميزات
* تغيير بنية
* تحسين UX
* إعادة تسمية أي شيء

---

## ✅ تعريف الانتهاء (Definition of Done)

Phase 0 تعتبر **منتهية** عندما:

* `Blockers = 0`
* الموقع شغّال 24 ساعة بدون crash
* Realtime شغّال
* Report مكتوب

---

### ⏭️ الخطوة التالية

بعد ما تخلص، اكتب لي حرفيًا:

```
Phase 0 DONE
```

وبننتقل فورًا إلى **Phase 1 — Core Ledger**
بنفس الأسلوب (وين تكتب + شو تنفّذ + برومبتات جاهزة).
