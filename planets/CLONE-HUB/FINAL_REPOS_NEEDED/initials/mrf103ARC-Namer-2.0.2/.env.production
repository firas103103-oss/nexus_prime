# Production Environment Variables
# Used for APK build and Production deployment

# API Configuration
VITE_API_URL=https://app.mrf103.com
VITE_APP_NAME=ARC Operator
VITE_APP_VERSION=2.0.0
VITE_ENVIRONMENT=production

# App Configuration
NODE_ENV=production
PORT=9002

# Note: Server-side variables (DATABASE_URL, API keys) should ONLY be in Railway dashboard
# These VITE_ variables are exposed to the client (APK)