{
  "name": "the-sultan-ai",
  "version": "1.0.0",
  "description": "The Sultan AI - Quranic Chat Application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "setup": "npm install && npm run build",
    "clean": "rm -rf backend/dist frontend/dist backend/node_modules frontend/node_modules node_modules",
    "test": "echo \"No tests specified\"",
    "docker:build": "docker build -t the-sultan-ai:latest .",
    "docker:run": "docker run -p 8080:8080 --env-file .env the-sultan-ai:latest",
    "docker:compose": "docker-compose up -d",
    "docker:compose:down": "docker-compose down"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
