# Shadow Seven - Environment Variables (Local-Only)
# Copy to .env and fill in values before build

# ═══ API Base URL (Local Backend) ═══
# Base URL for backend. Empty = same-origin. Or: https://publisher.mrf103.com
VITE_API_URL=

# JWT for PostgREST auth (if using login/register)
VITE_SUPABASE_ANON_KEY=

# ═══ AI (Ollama local - metadata extraction via LiteLLM) ═══
# VITE_GOOGLE_AI_API_KEY not needed for local-only

# ═══ SMTP (Ready-to-Fill) ═══
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
