/**
 * X-BIO OMEGA — WiFi & Ingest Configuration (Production)
 * BEFORE BUILD: Replace placeholders with your actual values.
 * Add src/arduino_secrets.h to .gitignore to avoid committing secrets.
 */
#ifndef ARDUINO_SECRETS_H
#define ARDUINO_SECRETS_H

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
#define XBIO_INGEST_URL "https://xbio.mrf103.com/api/ingest"

#endif
